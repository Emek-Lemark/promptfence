# PromptFence Environment Variables
# Copy this file to .env.local for local development

# Database path (optional, defaults to ./data/promptfence.db)
# For Railway with volume: /data/promptfence.db
DATABASE_PATH=./data/promptfence.db

# JWT signing secret (REQUIRED in production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here

# Node environment (set automatically by Railway)
NODE_ENV=development
